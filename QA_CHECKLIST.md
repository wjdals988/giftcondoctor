# 기프티콘닥터 MVP 수동 QA 체크리스트

## 인증
- [ ] 이메일/비밀번호 회원가입 성공
- [ ] 이메일/비밀번호 로그인 성공
- [ ] Google 로그인 성공
- [ ] 로그인 후 `users/{uid}` 문서 생성/갱신 확인
- [ ] 로그아웃 후 로그인 화면 복귀

## 푸시 토큰/권한
- [ ] Android 13+ 첫 로그인 화면에서는 알림 권한 시스템 창이 자동 노출되지 않음
- [ ] 로그인 후 쿠폰방이 있으면 `만료 알림 받기`를 탭했을 때만 알림 권한 요청 UI 노출
- [ ] 첫 실행 직후 `coupon_expiry` 알림 채널이 중요도 높음으로 생성
- [ ] 권한 허용 후 `users/{uid}/pushTokens/{tokenId}` 저장
- [ ] FCM token refresh 시 같은 경로가 갱신됨
- [ ] 알림 설정에서 pushEnabled 변경 저장

## 방
- [ ] 방 생성 성공
- [ ] 방장 member 문서와 사용자 roomMembership 문서 생성
- [ ] 6자리 초대코드 생성
- [ ] 초대코드로 다른 계정 입장 성공
- [ ] 만료/오류 초대코드는 입장 실패
- [ ] 멤버 목록 표시
- [ ] 방장이 일반 멤버 제거 가능
- [ ] 일반 멤버 방 나가기 가능
- [ ] 가입/탈퇴/멤버 제거 후 `memberCount`가 실제 멤버 수와 일치
- [ ] 등록한 쿠폰이 남은 멤버는 탈퇴/제거가 차단됨
- [ ] 방장은 방 나가기 대신 방 삭제 버튼이 표시됨
- [ ] 방 삭제 시 멤버십, 쿠폰, 댓글, Blob 이미지가 정리됨

## 쿠폰
- [ ] 이미지 선택 후 쿠폰 추가 성공
- [ ] 5MB 이상 이미지 선택 시 OCR과 업로드 준비가 병렬 완료되고 저장 버튼 직후 준비 대기 없이 전송이 시작됨
- [ ] 최적화된 이미지 업로드에서 원본→전송 용량이 표시되고 2,560px 상한에서도 바코드·작은 글자가 식별 가능함
- [ ] 작은 이미지 또는 절감률 10% 미만 이미지는 재압축하지 않고 원본 크기로 업로드됨
- [ ] 이미지 재선택·업로드 취소·앱 강제 종료 후 재실행 시 `coupon-upload-prepared` 임시 파일이 남지 않음
- [ ] 등록 성공 후 상세 화면에 완료 스낵바가 표시되고 `하나 더 등록`을 누르면 같은 방의 등록 화면으로 이동
- [ ] 10MB 이미지 업로드 중 진행률이 멈추지 않고 `업로드 취소` 후 정리 상태와 재시도 가능 상태가 순서대로 표시됨
- [ ] 업로드 응답 후 Firestore 저장 실패·화면 이탈 시 쿠폰 문서가 있으면 이미지를 보존하고, 문서가 없으면 즉시 삭제 또는 cleanup queue로 정리됨
- [ ] Blob 응답 유실·취소 요청 유실에도 upload session cleanup job이 남고 다음 Cron에서 live 경로만 보존함
- [ ] 썸네일 처리 실패 시 병렬 업로드된 원본 Blob이 보상 삭제됨
- [ ] Firestore에는 public URL이 아니라 `imageBlobPath`만 저장
- [ ] 상세 화면에서 인증 API를 통해 이미지 표시
- [ ] 방 공개 쿠폰은 멤버가 목록/상세에서 확인
- [ ] private 쿠폰은 등록자만 목록/상세/이미지 접근
- [ ] 예약, 예약 취소, 사용 완료 상태 변경
- [ ] 예약된 쿠폰은 예약자만 사용 완료할 수 있고 완료 후 `reservedByUid`가 비워짐
- [ ] 사용 완료 직후 `실행 취소`를 누르면 처리자 본인은 5분 안에 사용 가능으로 복원되고 타인·5분 경과 요청은 거부됨
- [ ] 쿠폰 수정 저장
- [ ] 등록자는 새 이미지 미리보기 후 쿠폰 이미지를 교체할 수 있고 다른 멤버에게는 교체 버튼이 보이지 않음
- [ ] 교체 이미지를 선택하면 확인 전 최적화가 완료되고 준비 중에는 교체 확인이 비활성화되며 취소·재선택은 가능함
- [ ] 교체 준비 완료 후 최적화 전후 용량 또는 원본 유지 사유가 표시되고 확인 직후 전송이 시작됨
- [ ] 이미지 교체 후 제목·만료일·예약 상태는 유지되고 원본·썸네일 경로와 화면 이미지만 갱신됨
- [ ] 이미지 교체 취소 시 기존 이미지와 Blob 경로가 유지됨
- [ ] 두 기기에서 동시에 이미지 교체 시 먼저 완료한 1건만 반영되고 충돌 요청의 새 Blob은 정리됨
- [ ] 이미지 교체 후 이전 Blob 삭제가 실패하면 `blobCleanupQueue`에 남고 다음 Cron에서 재시도됨
- [ ] 쿠폰 영구 삭제·방 삭제 후 Blob 삭제가 실패해도 API 삭제는 완료되고 cleanup queue가 남음
- [ ] Firestore 삭제가 중간 실패해 쿠폰 문서가 남으면 cleanup queue가 사용 중 Blob을 삭제하지 않음
- [ ] 잘못된 쿠폰 prefix의 cleanup 문서는 Blob을 삭제하지 않고 dead-letter 처리됨
- [ ] cleanup retry·dead-letter·만료 lease가 `/api/notifications/status`의 warning/critical에 반영됨
- [ ] 상세 이미지에서 핀치·더블탭과 1×~4× 확대/축소·원본 맞춤 버튼이 동작
- [ ] 목록에서 본 쿠폰의 상세·전체화면 확대가 썸네일로 즉시 열리고 원본 준비 후 같은 화면에서 선명하게 교체됨
- [ ] 원본 이미지 로딩 실패 시 확대 화면의 미리보기는 유지되고 `원본 다시 불러오기`가 동작
- [ ] 상세 이미지 다운로드 중 화면을 닫거나 재시도하면 부분 cache 파일이 삭제되고 이전 요청이 화면을 덮어쓰지 않음
- [ ] 상세 화면 종료·로그아웃·앱 재시작 후 `coupon-detail-images` cache에 이전 원본이 남지 않음
- [ ] 썸네일이 있는 쿠폰은 상세 화면만 열고 닫을 때 원본 HTTP 요청과 cache 파일이 생성되지 않음
- [ ] 썸네일을 눌러 확대하면 원본 요청이 1회만 시작되고 준비 중 확대를 닫으면 요청·부분 파일이 취소됨
- [ ] 전체화면 쿠폰에서 최대 밝기·화면 켜짐이 적용되고 닫으면 이전 밝기·화면 유지 상태가 복원됨
- [ ] QR·EAN·Code 128 쿠폰 이미지에서 바코드 형식·값이 감지되고 저장 전에 수정하거나 제외 가능
- [ ] 자동 감지 실패 시 6개 주요 형식을 직접 선택하고 값 오류(EAN 문자·1D 80자 초과)가 저장 전에 안내됨
- [ ] 저장한 바코드가 흰 배경 전체화면으로 재생성되고 최대 밝기에서 실제 매장 스캐너로 인식됨
- [ ] 이미지 교체 후 이전 이미지의 바코드 메타데이터가 제거되어 오래된 코드가 표시되지 않음
- [ ] 4K 이미지 분석 시 OCR과 바코드 결과가 표시되고 OOM·UI 멈춤이 발생하지 않음
- [ ] 같은 목록 썸네일을 빠르게 재구성해도 중복 HTTP 요청이 발생하지 않고, 두 번째 조회가 메모리 cache hit로 표시됨
- [ ] 쿠폰 삭제 직후 목록에서 사라지고 `실행 취소`를 누르면 이미지·댓글·상태가 복원됨
- [ ] 방 설정의 최근 삭제한 쿠폰에서 30일 남은 기간과 복원·영구 삭제 행동이 표시됨
- [ ] 공개 쿠폰은 등록자와 방장이 복구함에서 관리할 수 있고 비공개 쿠폰은 등록자에게만 표시됨
- [ ] 영구 삭제 확인 뒤 Firestore 문서·댓글·원본·썸네일이 삭제되고 더 이상 복원되지 않음
- [ ] 삭제 후 30일이 지난 쿠폰은 Cron에서 자동 영구 삭제되고 실패한 Blob 정리는 cleanup queue에 남음
- [ ] 쿠폰 상세에서 댓글 작성/목록 표시/내 댓글 삭제 가능
- [ ] private 쿠폰 댓글은 등록자만 접근 가능

## 알림
- [ ] 기본 모드 `[7,3,1,0]` 저장
- [ ] 최소/기본/꼼꼼 모드 저장
- [ ] 전체 푸시 알림 설정이 모든 방에 공통 적용됨
- [ ] 단말 푸시 알림 설정 테스트 버튼으로 즉시 테스트 푸시 수신
- [ ] 만료 알림 형식 테스트 버튼으로 `쿠폰 만료 D-Day` 푸시를 즉시 수신
- [ ] Cron 수동 호출이 summary JSON 반환
- [ ] Cron 일부 발송 실패 시 summary와 함께 HTTP 500 반환
- [ ] 같은 날짜 Cron을 동시에 2회 호출하면 1회만 lease를 획득하고 다른 1회는 `locked`로 종료
- [ ] 동일 수신자·쿠폰·만료일 outbox가 1개만 생성되고 전송 성공 후 `sent` 상태 유지
- [ ] 일시적 FCM 오류는 `retry`, 최대 5회 실패는 `deadLetter`, 만료된 `sending` lease는 재획득
- [ ] 무인증 알림 상태 조회는 401이고 정상 인증 시 상태별 건수·health·최근 Cron 결과 반환
- [ ] 완료된 outbox, notification log, Blob cleanup 기록만 30일 이후 일 200개 이하로 정리
- [ ] 알림 탭 시 `CouponDetailScreen`으로 이동

## 보안
- [ ] 비로그인 API 요청은 401
- [ ] 비멤버 이미지 조회는 403
- [ ] 비멤버 room/coupon read 차단
- [ ] private 쿠폰은 방장이라도 등록자가 아니면 이미지 조회 불가
- [ ] 쿠폰 등록자가 아닌 멤버의 이미지 교체 API는 403
- [ ] `.env*`, service account JSON, `google-services.json` 미커밋

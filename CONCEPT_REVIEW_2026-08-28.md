# 앱 컨셉과 사용자 친숙도 검토

기준일: 2026-08-28
검토 대상: `main` `0.1.32 (33)`
검증 환경: 실기기 SM_S948N(로그인 상태), 라우트·화면 구조 정적 분석

이 문서는 색·타이포 같은 표면 품질(`UIUX_REVIEW_2026-08-28.md`)이 아니라 **구조와 개념**을 다룬다. 개별 화면은 잘 만들어져 있는데 화면들이 조립된 방식이 사용자의 실제 사용 순서와 어긋나는 지점을 찾는다.

## 0. 요약

| 구조적 결함 | 근거 | 영향 |
| --- | --- | --- |
| 방을 만들어야 쿠폰을 저장할 수 있다 | 쿠폰은 `rooms/{roomId}/coupons` 하위에만 존재 | 첫 사용에서 관심 없는 개념을 먼저 배워야 함 |
| 내 쿠폰 전체를 볼 화면이 없다 | 앱에 `collectionGroup` 조회 없음. 검색·필터는 `RoomDashboard` 안에만 | 방이 여러 개면 "지금 쓸 쿠폰"을 찾으려 방을 순회해야 함 |
| 최상위 이동 수단이 없다 | `NavigationBar`/`BottomNavigation` 0건, 모든 라우트가 `rooms/...` 하위 | 화면 간 이동이 항상 뒤로가기 경유 |

세 가지는 같은 뿌리다. **"방(room)"이 데이터 모델이자 내비게이션 축이자 첫 화면**이라, 사용자가 방을 먼저 이해해야 앱이 시작된다.

## 1. 컨셉과 실제 사용의 어긋남

README가 정의하는 컨셉은 이렇다.

> Android-first MVP for Korean users to manage shared gifticon/coupon images in rooms and receive expiration push notifications.

핵심어는 **shared**와 **rooms**다. 그런데 기프티콘 사용의 실제 순서는 대체로 이렇다.

1. 카톡으로 기프티콘을 받는다 → **혼자** 저장한다
2. 만료 전에 알림을 받는다 → **혼자** 쓴다
3. (가끔) 가족·친구와 나눠 쓴다

즉 **공유는 나중에 생기는 필요**인데, 앱은 그것을 첫 관문으로 놓았다. 방을 만들지 않으면 쿠폰을 한 장도 저장할 수 없다.

현재 첫 저장까지의 경로는 다음과 같다.

```
로그인 → 방 목록(비어 있음) → 새 쿠폰방 만들기 → 방 이름 입력 → 만들기
      → 방 상세 → 쿠폰 등록 → 이미지 선택 → 정보 확인 → 저장
```

"쿠폰 하나 저장해 두고 알림 받고 싶다"는 사용자가 **방 이름을 뭐라고 지을지부터** 결정해야 한다. 이 결정은 사용자에게 아무 가치도 주지 않는다.

`visibility = private`(나만 보기)가 이미 있으므로 **혼자 쓰는 사용은 이미 지원된다.** 문제는 그 사용이 여전히 방을 요구한다는 점이다.

### 개선 방향

가장 작은 변경은 **가입 시 개인 보관함을 자동 생성**하는 것이다.

- 첫 로그인 시 "내 쿠폰" 방을 자동으로 만들고 바로 그 안으로 보낸다
- 방 목록은 이 보관함이 항상 맨 위에 오도록 한다
- 방 만들기·초대코드 입장은 그대로 두되, 필수 관문이 아니라 선택지가 된다

데이터 모델을 바꾸지 않으므로(여전히 room 하나) Firestore rules, cron, 알림 로직에 영향이 없다. 사용자 입장에서만 "방"이라는 개념이 뒤로 밀린다.

## 2. 내 쿠폰 전체를 볼 화면이 없다

앱에서 쿠폰을 보려면 반드시 방에 들어가야 한다. 검색(`coupon-search`)과 상태 필터도 `RoomDashboard` 안에만 있다. 방이 3개면 "이번 주에 만료되는 쿠폰"을 확인하려고 방 3개를 각각 열어야 한다.

이 공백이 특히 눈에 띄는 이유는, **서버는 이미 방을 가로질러 계산하고 있기 때문이다.**

- `backend/app/api/cron/expiry-reminders/route.ts:632` — `db.collectionGroup("coupons")` 로 전체 만료 쿠폰 조회
- 그 결과를 푸시로 보낸다

즉 "당신의 만료 임박 쿠폰"이라는 개념은 서버에 이미 존재하는데 앱에는 그 화면이 없다. 사용자는 알림으로만 그 사실을 알 수 있고, 알림을 놓치면 방을 순회해야 한다.

딥링크는 `roomId` + `couponId`로 특정 쿠폰까지 직행하므로(`cron/expiry-reminders/route.ts:238-245`) **알림을 탭한 경우는 문제없다.** 문제는 사용자가 알림 없이 앱을 직접 열었을 때다. 매장 앞에서 "커피 쿠폰 있었나?" 하고 열었을 때 검색할 곳이 없다.

### 개선 방향

방 목록 화면 상단에 **만료 임박 요약**을 둔다.

- "이번 주 만료 3개" 같은 한 줄과 탭하면 열리는 목록
- 데이터는 `collectionGroup` 대신 이미 구독 중인 방들의 쿠폰을 합쳐 구성할 수 있다(방 목록은 실시간이고 각 방의 pager 도 실시간이다)
- Firestore 규칙 변경 없이 클라이언트 합산으로 시작하고, 방 개수가 많아지면 서버 집계로 옮긴다

## 3. 최상위 이동 수단이 없다

`NavigationBar`·`BottomNavigation`·`NavigationRail` 사용이 0건이다. 모든 화면이 `rooms/...` 아래 중첩되고, 알림 설정과 앱 정보만 `settings/...`로 빠져 있다.

결과적으로 화면 이동이 전부 **뒤로가기 경유**다. 쿠폰 상세에서 다른 방의 쿠폰을 보려면 뒤로 3번을 눌러야 한다.

다만 이건 **지금 당장 고칠 문제는 아니다.** 하단 탭은 목적지가 3개 이상일 때 의미가 있는데, 현재 최상위 목적지는 사실상 "방 목록" 하나다. 2절의 만료 임박 화면이 생기면 그때 탭이 정당해진다.

순서는 이렇게 잡는 편이 낫다. 1절(개인 보관함) → 2절(만료 임박 요약) → 그다음에 탭 도입을 검토한다. 목적지가 없는 상태에서 탭부터 넣으면 빈 서랍을 만드는 셈이다.

## 4. 알림 권한 안내가 묻힌다

2026-08-28 실기기 확인에서 푸시가 오지 않는다는 신고의 원인은 **`POST_NOTIFICATIONS` 권한 미부여** 하나였다. 서버·FCM·Firebase 설정은 모두 정상이었고, 권한을 부여하자 두 종류의 테스트 푸시가 즉시 도착했다.

앱은 이미 이 상태를 감지해 방 목록 상단에 배너를 띄우고 있었다.

> 만료 푸시 알림 · 꺼짐
> 쿠폰 만료 알림을 받으려면 먼저 기기 알림을 허용해 주세요.
> [만료 알림 받기]

그런데도 사용자가 놓쳤다. 배너가 방 목록 카드들과 같은 시각적 무게를 갖고 있어서 목록의 일부처럼 읽히기 때문이다.

이 앱은 **알림이 존재 이유**다. 알림이 꺼져 있으면 앱의 핵심 기능이 0%로 동작한다. 그 상태를 다른 정보와 같은 비중으로 표시할 이유가 없다.

### 개선 방향

- 권한이 없을 때는 배너를 `errorContainer` 계열로 올려 "지금 앱이 제 기능을 못 한다"는 신호를 준다
- 쿠폰을 처음 저장한 직후에 권한을 요청한다. 지금은 이미 "로그인 전에 묻지 않고 첫 쿠폰방 이후" 로 늦춰 놓았는데(0.1.20 변경 이력), 한 단계 더 미뤄 **저장 성공 직후** 가 가장 설득력 있는 시점이다. "이 쿠폰 만료 3일 전에 알려드릴까요?" 는 거절하기 어려운 제안이다

## 5. 그 밖에 관찰된 것

### OCR 이 이름을 못 찾으면 바코드 번호가 제목이 된다

실기기에서 쿠폰 제목이 `9313 3685 7353 5282` 로 저장된 항목을 확인했다. OCR 이 상품명을 찾지 못하고 바코드 숫자를 제목으로 채운 결과다. 목록에서 이 쿠폰이 무엇인지 알 방법이 없다.

`TODO_v1.1.md` P1 의 "OCR 다중 날짜·저화질 이미지 정확도 개선" 과 같은 뿌리지만, 정확도를 올리기 전에 할 수 있는 방어가 있다. **제목이 숫자와 공백만으로 이루어져 있으면 제목으로 채택하지 않고 비워 둔다.** 비어 있으면 사용자가 직접 입력하게 되고, 잘못된 제목보다 낫다.

### 변경 이력이 앱 안에서 0.1.20 에 멈춰 있었다

앱 정보 화면의 변경 이력이 0.1.20 이후 갱신되지 않았다. 0.1.32 항목을 추가했다(PR #43). 릴리스마다 갱신하는 절차가 없으면 다시 벌어진다.

## 6. 우선순위

### P0 — 핵심 기능이 동작하지 않는 상태를 막는다

1. 알림 권한 미부여 배너를 경고 수준으로 격상 (4절)
2. 첫 쿠폰 저장 직후로 권한 요청 시점 이동 (4절)

### P1 — 첫 사용과 재방문의 장벽

3. 가입 시 개인 보관함 자동 생성 (1절)
4. 방 목록 상단 만료 임박 요약 (2절)
5. OCR 제목이 숫자뿐이면 비워 두기 (5절)

### P2 — 구조

6. 목적지가 2개 이상이 된 뒤 하단 탭 검토 (3절)

## 7. 비관적 관점

1절의 개인 보관함은 데이터 모델을 바꾸지 않지만 **기존 사용자에게는 방이 하나 늘어난다.** 이미 방을 만들어 쓰던 사용자에게 빈 "내 쿠폰" 방이 생기면 오히려 혼란이다. 신규 가입자에게만 적용하거나, 기존 사용자에게는 만들지 않는 분기가 필요하다.

2절의 만료 임박 요약을 클라이언트 합산으로 만들면 **구독 중인 방의 쿠폰만 집계된다.** 페이징 때문에 아직 불러오지 않은 쿠폰은 빠진다. 정확한 수치를 보이려면 서버 집계가 필요한데, 그러면 새 엔드포인트와 Firestore 인덱스가 늘어난다. 처음부터 "불러온 범위 기준" 이라고 표기하고 시작하는 편이 안전하다.

4절의 권한 요청 시점 이동은 **거절 후 재요청이 어렵다는 Android 제약**과 부딪힌다. 두 번 거절하면 시스템이 다시 묻지 않으므로, 요청 시점을 옮기는 것만으로는 이미 거절한 사용자를 되돌릴 수 없다. 그 사용자에게는 설정 앱으로 보내는 경로가 따로 필요하다.

## 8. 긍정적 관점

구조를 바꾸는 제안이지만 **데이터 모델과 서버는 그대로 둘 수 있다.** 개인 보관함은 room 하나를 자동 생성하는 것이고, 만료 임박 요약은 이미 실시간으로 구독 중인 데이터를 합치는 것이다. Firestore rules, cron, 알림 outbox 같은 검증된 부분을 건드리지 않는다.

그리고 서버는 이미 "사용자의 만료 임박 쿠폰" 을 계산하고 있다. 앱에 없는 것은 그 개념을 보여줄 화면뿐이다. 만들어야 할 것이 아니라 **드러내야 할 것**에 가깝다.

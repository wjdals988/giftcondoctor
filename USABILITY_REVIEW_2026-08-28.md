# 사용성·편의성 강화 항목

기준일: 2026-08-28
검토 대상: `main` `0.1.33 (34)` + 미병합 PR #55
근거: 화면·라우트·데이터 계층 정적 분석, 실기기 SM_S948N 조작

앞선 두 문서와 겹치지 않는 영역을 본다. `UIUX_REVIEW_2026-08-28.md`는 색·타이포·정보 위계, `CONCEPT_REVIEW_2026-08-28.md`는 구조와 온보딩을 다뤘다. 이 문서는 **이미 쓰고 있는 사용자가 매일 겪는 마찰**을 다룬다.

## 0. 판단 기준

이 앱의 가치가 실현되는 순간은 두 개뿐이다.

1. **저장하는 순간** — 카톡으로 기프티콘을 받고 앱에 넣는다
2. **쓰는 순간** — 매장 계산대에서 바코드를 보여준다

나머지는 전부 이 둘을 보조한다. 따라서 이 두 순간에 걸리는 마찰이 가장 비싸다. 아래 항목은 그 기준으로 정렬했다.

## 1. 매장에서 바코드까지 4탭이 걸린다

가장 급한 순간의 경로가 가장 길다.

```
앱 실행 → 방 목록 → 방 선택 → 쿠폰 선택 → 바코드 크게 보기
```

계산대 앞에서 뒷사람이 기다리는 상황이다. 방이 여러 개면 어느 방에 넣었는지도 기억해야 한다.

비교 대상이 되는 상황을 보면 이 경로가 왜 문제인지 분명해진다. 지금 이 앱에는 **다음 세 가지가 모두 없다**.

| 빠른 접근 수단 | 현황 |
| --- | --- |
| 홈 화면 위젯 | `AppWidget` 관련 코드 **0건** |
| 앱 바로가기(long-press shortcut) | `ShortcutManager` **0건** |
| 즐겨찾기·핀 | `favorite`/`pinned` **0건** |

### 개선 방향

가장 싼 것부터 세 단계로 나눌 수 있다.

**(a) 최근 사용 쿠폰 바로가기** — `ShortcutManager.pushDynamicShortcut` 으로 최근 연 쿠폰 3개를 앱 아이콘 길게 누르기에 노출한다. 앱을 실행하지 않고 딥링크로 바로 상세에 진입한다. 딥링크 경로(`rooms/{roomId}/coupons/{couponId}`)가 이미 있으므로 배선만 하면 된다.

**(b) 즐겨찾기** — 자주 쓰는 쿠폰(회사 앞 카페 등)을 목록 최상단에 고정한다. `coupons` 문서에 `favoritedBy` 배열을 두면 되지만 Firestore 규칙 변경이 따른다. 사용자별 값이므로 `users/{uid}/favorites/{couponId}` 하위 컬렉션이 규칙 변경 없이 더 안전하다.

**(c) 홈 위젯** — 만료 임박 쿠폰 1~3개를 홈 화면에 띄우고 탭하면 바코드로 직행한다. Glance(Compose for Widgets)를 쓰면 기존 컴포저블 자산을 일부 재사용할 수 있다. 다만 위젯은 별도 프로세스에서 Firestore 를 읽어야 하므로 인증·캐시 설계가 필요하다. 세 항목 중 비용이 가장 크다.

(a)만으로도 4탭이 1탭이 된다. 먼저 할 일이다.

## 2. 오프라인에서 쓸 수 있는데 그렇다고 말하지 않는다

매장은 지하이거나 데이터가 약한 경우가 많다. 확인해 보니 **핵심 경로는 이미 오프라인에서 동작할 조건을 갖추고 있다.**

| 구성 요소 | 오프라인 가용성 | 근거 |
| --- | --- | --- |
| 쿠폰 메타데이터 | 가능 | Firestore Android SDK 는 오프라인 캐시가 기본 활성 |
| 바코드 값 | 가능 | `Coupon.barcodeValue` 로 문서에 저장됨(`Models.kt:95`) |
| 바코드 이미지 | 가능 | `renderCouponBarcode` 가 ZXing 로컬 렌더링(`CouponBarcodeRenderer.kt:8`) |
| 쿠폰 원본 이미지 | **불가** | 메모리 `LruCache` 뿐. 디스크 캐시 없음 |

즉 **바코드가 저장된 쿠폰은 네트워크 없이도 계산대에서 쓸 수 있다.** 그런데 앱은 이 사실을 어디에서도 말하지 않고, 오프라인 상태에서 무엇이 되고 무엇이 안 되는지 구분해 주지도 않는다.

사용자 입장에서는 "데이터 안 터지면 이 앱은 못 쓴다" 고 학습하게 되고, 실제로는 쓸 수 있는데도 원본 이미지 로딩 실패 화면을 보고 포기할 수 있다.

### 개선 방향

- 오프라인일 때 쿠폰 상세에서 **바코드 버튼을 먼저 강조**하고, 원본 이미지 영역에는 "지금은 네트워크가 없어 원본을 불러올 수 없어요. 바코드는 그대로 사용할 수 있습니다" 를 표시한다
- 바코드 값이 없는 쿠폰만 "네트워크 연결이 필요합니다" 로 안내한다
### 실기기 비행기 모드 실측 (2026-08-28)

추론으로 남기지 않고 SM_S948N 을 비행기 모드로 두고 확인했다.

| 확인 항목 | 결과 |
| --- | --- |
| 앱 실행 | 정상 (COLD 468ms) |
| 방 목록 | 4개 모두 표시 |
| 쿠폰 목록 | "불러온 6개 중 6개" 전부 표시 |
| 쿠폰 상세 진입 | 제목·브랜드·상태·만료일 모두 표시 |
| 원본 이미지 | 실패 |

**Firestore 오프라인 캐시가 실제로 동작한다.** 메타데이터 경로는 네트워크 없이 완전히 살아 있다.

다만 **바코드까지는 확인하지 못했다.** 시험한 쿠폰에 `barcodeValue` 가 없어 버튼 자체가 렌더링되지 않았다(자동 감지가 실패했고 제목에 바코드 번호가 들어간 그 쿠폰이다). 바코드가 저장된 쿠폰으로 다시 확인해야 이 절의 핵심 주장이 완성된다.

### 실측 중 발견한 결함 — 영어 예외 메시지 노출

오프라인 상세 화면이 이 문구를 그대로 보여줬다.

```
A network error (such as timeout, interrupted connection or unreachable host) has occurred.
```

한국어 앱에 **Firebase SDK 의 영어 원문 예외 메시지**가 사용자 화면에 노출된다. 사용자는 이게 무슨 뜻인지, 무엇을 해야 하는지 알 수 없다. 그리고 이 화면에서 실제로 중요한 사실 — **쿠폰 정보는 멀쩡히 보이고 있다는 것** — 은 아무도 말해 주지 않는다.

원인은 오류를 `localizedMessage` 로 그대로 흘리는 경로다. 이번 세션에서 Google 로그인에 같은 문제가 있었고(PR #40) 상태 코드를 문구로 옮겨 해결했다. 이미지 로딩도 같은 처방이 필요하다.

이 항목은 2절의 개선 방향에 흡수해 함께 고치는 편이 낫다. 오프라인 안내를 붙이는 자리와 정확히 같은 곳이다.

## 3. 정렬을 바꿀 수 없다

목록 정렬이 만료 임박순 하나로 고정돼 있다(`CouponListRules.kt:25-29`). 화면에도 "만료 임박순" 이라고 적혀 있지만 바꿀 수단이 없다.

정렬 자체는 잘 설계돼 있다. 사용 가능 → 만료 → 사용 완료 순으로 묶고, 진행 중인 것은 임박순, 끝난 것은 최신순으로 정렬한다. 기본값으로는 최선이다.

문제는 다른 필요가 생겼을 때다.

- "지난달에 쓴 스타벅스 쿠폰이 뭐였지" — 사용 완료를 최신순으로 보고 싶다. 지금은 필터로 좁힐 수는 있으나 정렬은 여전히 고정
- "브랜드별로 모아 보고 싶다" — 불가능

### 개선 방향

우선순위는 낮다. 필터가 이미 5종(전체·사용 가능·예약·사용 완료·만료) 있어서 대부분의 탐색 요구는 필터로 처리된다. **정렬 추가보다 2절(오프라인)과 1절(빠른 접근)이 먼저다.**

굳이 한다면 브랜드 그룹핑보다 "이름순" 하나를 더하는 편이 낫다. 같은 브랜드 쿠폰이 여러 장일 때 이름순이 사실상 브랜드 그룹핑이 된다.

## 4. 카메라로 바로 찍어 등록할 수 없다

등록 경로가 두 개인데 둘 다 **이미 갤러리에 있는 이미지**를 전제한다.

- 갤러리 선택(`PickVisualMedia`) — 최대 10장
- 공유 시트(`ACTION_SEND`/`SEND_MULTIPLE`) — 카톡에서 바로 넘기기

`ACTION_IMAGE_CAPTURE`/`TakePicture` 사용은 **0건**이다.

종이 쿠폰, 영수증에 인쇄된 쿠폰, 다른 사람 화면에 뜬 기프티콘을 찍어서 넣는 경로가 없다. 사용자는 카메라 앱으로 나가서 찍고 → 갤러리에 저장되고 → 앱으로 돌아와 선택해야 한다.

### 개선 방향

등록 화면의 "갤러리에서 쿠폰 선택" 옆에 "촬영" 을 추가한다. `ActivityResultContracts.TakePicture` 는 결과를 지정한 `Uri` 에 쓰므로, 기존 `SharedImageImportStore` 의 임시 파일 흐름을 그대로 재사용할 수 있다.

다만 이 항목은 **실제 수요를 확인하고 진행하는 편이 낫다.** 국내 기프티콘은 대부분 카톡·앱으로 오므로 촬영 수요가 크지 않을 수 있다. 종이 쿠폰을 쓰는 사용자가 있는지가 판단 근거다.

## 5. 검색이 방 안에서만 동작한다

`CONCEPT_REVIEW` 2절에서 만료 임박 요약으로 일부 해소했지만, **검색 자체는 여전히 방 단위**다(`RoomScreens.kt:812` `coupon-search`).

"스타벅스" 를 찾으려면 방을 하나씩 열어 각각 검색해야 한다. 만료 임박 요약은 7일 이내만 보여주므로 검색을 대체하지 못한다.

### 개선 방향

`GET /api/coupons/expiring` 를 만들 때 세운 구조를 그대로 확장할 수 있다. 서버가 사용자의 방 목록 범위에서 이름·브랜드로 검색해 돌려주는 엔드포인트를 추가하고, 방 목록 상단 검색창에서 호출한다.

주의할 점은 두 가지다. 남이 등록한 비공개 쿠폰은 결과에서 빼야 하고(이미 `selectExpiringCoupons` 에 같은 규칙이 있다), 결과가 잘렸으면 그 사실을 표시해야 한다.

## 6. 되돌리기가 되는데 되돌릴 방법이 화면에 없다

실행 취소 경로는 **상세 화면에 머무는 동안의 스낵바**뿐이다(`CouponScreens.kt:926` `usedFeedbackVersion` 은 화면 로컬 `rememberSaveable`). 계산대에서 사용 완료를 누르고 앱을 닫은 뒤 "잘못 눌렀다" 는 것을 깨달으면 되돌릴 방법이 없다.

### 확인 결과 — 능력은 이미 있다

`undoMarkUsed` 를 열어 보니 조건이 둘뿐이다(`CouponRepository.kt:265-280`).

```kotlin
if (snapshot.getString("status") != "used") error(...)
if (snapshot.getString("usedByUid") != uid) error(...)
```

**시간 제한이 없다.** 즉 상태가 `used` 이고 처리자가 본인이면 언제든 되돌릴 수 있다. 막고 있는 것은 서버 규칙이 아니라 UI 진입점의 부재다.

`usedAt` 과 `usedByUid` 는 Firestore 에 저장되고 있다(`CouponRepository.kt:258-259`). 다만 `usedAt` 은 `Coupon` 모델에 노출돼 있지 않아 클라이언트가 "언제 사용했는지" 를 알 수 없다.

### 문서와 구현의 불일치

`TODO_v1.1.md` 와 `UX_BENCHMARK` 는 이 기능을 "사용 완료 직후 5분 내 처리자 본인 실행 취소" 로 기록하고 있다. 처리자 제한은 구현돼 있지만 **5분 제한은 어디에도 없다.** 둘 중 하나를 맞춰야 한다.

무제한 되돌리기가 나쁜 것은 아니다. 공유 방에서 A 가 사용 완료로 바꾼 것을 한참 뒤에 되돌리면 B 의 화면이 뒤집히지만, 애초에 A 만 되돌릴 수 있으므로 악용 여지는 작다. 다만 **문서가 없는 제약을 기술하고 있다는 사실 자체가 위험**하다. 나중에 이 문서를 근거로 판단하면 틀린 전제 위에서 설계하게 된다.

### 개선 방향

- 상세 화면에 재진입했을 때 `status == "used" && usedByUid == 본인` 이면 "사용 완료로 표시했어요 · 실행 취소" 를 **인라인 요소**로 노출한다. 스낵바가 아니어야 앱을 다시 열어도 보인다
- `usedAt` 을 `Coupon` 모델에 추가해 "3분 전 사용" 처럼 시점을 함께 보여준다
- 5분 제한을 정말 둘 것인지 먼저 결정한다. 두기로 하면 서버(또는 리포지토리)에 넣어야 하고, 두지 않기로 하면 문서를 고쳐야 한다

## 7. 우선순위

### 지금 하면 좋은 것

1. **오프라인 안내와 영어 예외 메시지 제거** (2절) — 이미 동작하는 것을 말해 주는 일이고, 실측에서 드러난 영어 원문 노출도 같은 자리에서 함께 고칠 수 있다
2. **최근 쿠폰 앱 바로가기** (1절 a) — 딥링크가 이미 있어 배선만 하면 4탭이 1탭이 된다
3. **사용 완료 실행 취소를 화면 요소로** (6절) — 서버 능력은 이미 있고 UI 진입점만 없다. 함께 문서·구현 불일치도 해소한다

### 그다음

4. 전체 쿠폰 검색 (5절) — 만료 임박 API 구조 재사용
5. 즐겨찾기 (1절 b)

### 수요 확인 후

6. 카메라 촬영 등록 (4절)
7. 정렬 옵션 (3절)
8. 홈 위젯 (1절 c)

## 8. 비관적 관점

1절과 2절은 **모두 매장이라는 실사용 맥락을 가정**하고 있는데, 이 문서를 쓰면서 실제 매장에서 앱을 써 본 적은 없다. "계산대에서 4탭은 길다" 는 판단은 그럴듯하지만 측정된 값이 아니다. 실제로는 사용자가 미리 앱을 열어 두는 습관을 갖고 있어 문제가 되지 않을 수도 있다.

2절은 작성 중 실기기 비행기 모드로 확인해 추론을 사실로 바꿨다. 메타데이터·목록·상세는 오프라인에서 동작한다. **다만 바코드 표시는 아직 확인하지 못했다** — 시험한 쿠폰에 `barcodeValue` 가 없어 버튼이 렌더링되지 않았다. 이 절의 핵심 주장("바코드가 있으면 오프라인에서 쓸 수 있다")은 여전히 미검증이며, 확인 없이 사용자에게 "오프라인에서 됩니다" 를 안내하면 거짓말이 될 수 있다.

6절은 작성 중 확인해 내용을 정정했다. `usedAt`·`usedByUid` 는 이미 저장되고 있고 `undoMarkUsed` 에 시간 제한도 없다. 즉 예상보다 싼 작업이다. 다만 그 과정에서 **문서가 실재하지 않는 5분 제한을 기술하고 있다는 것**이 드러났다. 이런 불일치가 다른 곳에도 있을 수 있으며, 이 문서 역시 같은 오류를 범했을 가능성이 있다.

## 9. 긍정적 관점

이 앱은 이미 어려운 부분을 해결해 두었다. 바코드를 로컬에서 재생성하고, 값을 문서에 저장하고, 딥링크를 쿠폰 단위까지 뚫어 놓았다. 1절과 2절은 새 기능을 만드는 일이 아니라 **이미 있는 것을 꺼내 쓰는 일**이다.

특히 딥링크는 푸시 알림용으로 만들어졌지만 앱 바로가기에도 그대로 쓸 수 있다. 만료 임박 API 도 검색 엔드포인트의 뼈대가 된다. 앞선 작업이 다음 작업의 비용을 낮추고 있다.

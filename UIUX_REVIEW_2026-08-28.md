# UI/UX 가독성·가시성 검토와 변경 목록

기준일: 2026-08-28
검토 대상: `main` `0.1.31 (32)`
검증 환경: emulator-5554 / emulator-5556(android-36.1) / 실기기 SM_S948N
근거: `android/app/src/main/java/com/giftcondoctor/app` 정적 분석, WCAG 2.2 대비율 계산, 200% 글꼴 실측 스크린샷

기존 `UX_BENCHMARK_2026-08-14.md`는 등록·공유·성능 흐름을 다뤘다. 이 문서는 그 문서가 `[예정]`으로만 남겨둔 **색·타이포·다크모드·정보 위계** 영역을 수치로 확정한다.

## 0. 요약

측정 가능한 결함 3개가 출시 차단급이다.

| 결함 | 측정값 | 기준 | 판정 |
| --- | --- | --- | --- |
| Primary 버튼 대비 | **2.60:1** | WCAG AA 텍스트 4.5:1 | 실패 (기준의 58%) |
| 입력 필드 외곽선 대비 | **1.22:1** | WCAG UI 요소 3.0:1 | 실패 (기준의 41%) |
| 다크 테마 | `darkColorScheme` **0건** | 시스템 테마 대응 | 미구현 |

가장 많이 눌리는 컨트롤(`Google로 바로 시작`, `로그인`)이 대비 기준을 통과하지 못한다.

## 1. 색 대비 실측

`GDTheme.kt`의 색상값으로 상대휘도를 계산한 결과다.

| 조합 | 대비 | AA 4.5:1 | UI 3.0:1 |
| --- | --- | --- | --- |
| 흰 텍스트 / `primary #00B4A6` | 2.60:1 | 실패 | 실패 |
| `primary #00B4A6` 텍스트 / 흰 배경 | 2.60:1 | 실패 | 실패 |
| `GDAccent #FFC247` / 흰 배경 | 1.61:1 | 실패 | 실패 |
| `outline #E2E6EA` / `background #FBFCFE` | 1.22:1 | 실패 | 실패 |
| `tertiary #0095D6` / 흰 배경 | 3.35:1 | 실패 | 통과 |
| `error #E5484D` / 흰 배경 | 3.91:1 | 실패 | 통과 |
| `onPrimaryContainer #007A73` / `primaryContainer #E7F8F4` | 4.75:1 | 통과 | 통과 |
| `onSurfaceVariant #616A73` / `background` | 5.36:1 | 통과 | 통과 |
| `onSurface #1A1C1E` / `background` | 16.65:1 | 통과 | 통과 |

본문 색은 건강하고, **강조색 계열만 전부 실패**한다. `#00B4A6`은 브랜드 색으로 유지하되 텍스트·아이콘을 얹는 용도로는 쓸 수 없다.

### 권장 수정값

| 토큰 | 현재 | 권장 | 근거 |
| --- | --- | --- | --- |
| `primary` (버튼 배경) | `#00B4A6` | `#00786F` | 흰 텍스트 대비 5.36:1로 AA 통과. 색조 유지 |
| `primary` (흰 배경 위 텍스트) | `#00B4A6` | `#006B63` | 대비 6.40:1 |
| `outline` | `#E2E6EA` | `#8A9299` | 대비 3.08:1로 UI 최소 기준 통과 |
| `error` | `#E5484D` | `#C4262B` | 대비 5.74:1 |
| `tertiary` | `#0095D6` | `#0070A3` | 대비 5.46:1 |
| `GDAccent` | `#FFC247` | 배경 전용으로 한정 | 텍스트로 쓸 수 있는 밝기가 아님 |

`#00B4A6`은 `primaryContainer` 계열 배경, 일러스트, 브랜드 로고에만 남긴다.

## 2. 다크 테마 미구현

```
isSystemInDarkTheme / darkColorScheme / dynamicColor 등장 횟수: 0
```

`GDTheme`은 `lightColorScheme` 하나만 넘긴다. 시스템 다크 모드 사용자는 야간에 전체 화면이 `#FBFCFE`로 발광한다. 쿠폰 앱은 매장 계산대에서 화면을 켜는 앱이고, 이미 전체화면 쿠폰에서 **최대 밝기 강제** 기능이 들어가 있다(`TODO_v1.1.md` 완료 항목). 밝기를 강제로 올리는 앱이 다크 테마가 없으면 야간 사용 시 눈부심이 그대로 노출된다.

Material You 동적 색상(`dynamicDarkColorScheme`)도 미적용이다.

**(2026-08-28 정정)** 적용하지 않기로 판단했다. 근거는 세 가지다.

첫째, 이 문서의 대비 검증 전체가 무효가 된다. 1절과 2절에서 primary 5.36:1, outline 3.08:1, error 5.74:1, tertiary 5.46:1 을 확정했는데 동적 색상은 이 값을 사용자 배경화면에서 유도한 색으로 교체한다. 검증한 수치가 기기마다 달라진다.

둘째, 스켈레톤이 깨진다. `GDSkeletonLight`(#DDE3E9)는 배경 #FBFCFE 대비 1.17~1.26 이 되도록 맞춘 값이다. 배경이 동적으로 바뀌면 이 쌍이 유지되지 않아 스켈레톤이 다시 보이지 않거나 반대로 거슬리게 된다. `GDExpiryBadge` 와 카테고리 아이콘은 컨테이너·콘텐츠가 모두 고정 쌍이라 자체적으로는 안전하지만, 이들이 얹히는 `surface` 가 바뀌면 카드 경계 대비가 달라진다.

셋째, 긴급도 색은 의미 기반이라 동적화할 수 없다. 만료 임박이 배경화면에 따라 초록이 되면 안 된다. 즉 동적 색상을 켜도 화면의 상당 부분은 고정색으로 남아, 결과적으로 두 색 체계가 섞인 화면이 된다.

브랜드 색(`GDBrand` #00B4A6)을 유지하는 것도 제품 결정 사항이지 UI 다듬기가 아니다. 동적 색상을 도입하려면 위 세 가지를 함께 설계해야 하며 이 문서의 범위를 넘는다.

## 3. 타이포그래피 미정의

```
MaterialTheme(colorScheme = ..., shapes = ...)  // typography 인자 없음
Typography( 정의 횟수: 0
```

M3 기본 타이포그래피는 라틴 문자 기준으로 행간이 설계돼 있다. 한글은 받침 때문에 실제 글자 높이가 라틴보다 크고, 같은 `lineHeight`에서 행간이 좁아 보인다. 200% 글꼴 실측 스크린샷에서 본문 3줄이 서로 붙어 보이는 원인이다.

권장: `bodyLarge`/`bodyMedium`의 `lineHeight`를 폰트 크기의 **1.5배 이상**으로 재정의하고, `lineBreak = LineBreak.Paragraph`와 `hyphens = Hyphens.None`을 지정해 한글 어절 단위 줄바꿈을 강제한다.

## 4. 모양 토큰 붕괴

```kotlin
extraSmall = 6.dp, small = 8.dp, medium = 8.dp, large = 8.dp, extraLarge = 8.dp
```

`small`부터 `extraLarge`까지 **4개 단계가 모두 8dp**다. M3 기본값은 `medium 12dp / large 16dp / extraLarge 28dp`로, 이 위계가 카드·다이얼로그·바텀시트·FAB의 시각적 층위를 만든다. 지금은 모든 요소가 같은 각도로 잘려 있어 "카드 안의 카드"가 구분되지 않는다. 실제로 로그인 화면의 안내 카드와 입력 필드가 동일한 8dp라 계층이 읽히지 않는다.

권장: `medium 12dp / large 16dp / extraLarge 24dp`. 브랜드가 각진 느낌을 원한다면 전체를 낮추되 **단계 차이는 유지**한다.

## 5. 정보 위계 — 쿠폰 상세가 라벨 덤프다

`CouponScreens.kt:1199-1205`

```kotlin
Text(coupon.title, style = headlineSmall)
Text(coupon.brand.ifBlank { "브랜드 없음" })
Text("만료일: ${coupon.expiresLocalDate} (${coupon.timezone})")
Text("상태: ${statusLabel(coupon.status)}")
Text("공개범위: ${if (coupon.visibility == "private") "비공개" else "방 공개"}")
Text("알림대상: ${if (coupon.notifyTarget == "ownerOnly") "등록자" else "전체 멤버"}")
```

문제 4가지가 겹쳐 있다.

1. **상세 화면에 D-day가 없다.** (2026-08-28 정정) 최초 검토에서 "D-day가 없다"고 적었으나 사실과 다르다. `RoomScreens.kt:850`의 쿠폰 목록에는 `couponDdayText`로 만든 D-day 배지가 이미 있었고, 이 함수는 서버 `status`가 `used`/`expired`일 때 계산값보다 우선하도록 이미 처리하고 있었다. 결함은 **상세 화면에만 D-day가 없다**는 것이고, 상세는 `2026-09-01` 같은 절대 날짜만 보여줘 사용자가 날짜를 직접 계산해야 했다. 한국 기프티콘 앱(카카오톡 선물함, 기프티스타, 니콘내콘 등)이 공통적으로 채택한 표현은 `D-3` 또는 `3일 남음` 배지이고, 이건 업계 관행이라기보다 이 앱의 핵심 가치와 직결된 표현이다.
2. **긴급도 색 인코딩이 없다.** 목록의 D-day 배지는 `GDBadge` 기본색(primaryContainer) 하나만 써서 D-1과 D-30이 같은 색이다. 만료 임박·만료됨·사용완료도 같은 검정 텍스트다. 상태를 색+아이콘+텍스트 3중으로 인코딩해야 색각 이상 사용자도 구분할 수 있다.
3. **`(Asia/Seoul)` 노출.** 타임존 식별자는 내부 구현이다. 국내 단일 시장 앱에서 사용자에게 보일 이유가 없다.
4. **6줄이 모두 동일한 `bodyLarge`.** 시선 유도 순서가 없어 스캔이 불가능하다.

권장 재구성: 상단에 쿠폰 이미지 + `D-3` 배지 → 브랜드/이름 → **바코드 크게 보기 단일 primary CTA** → 부가정보는 접이식 섹션.

**(2026-08-28 정정)** "지금은 `예약`/`예약 취소`가 CTA 자리를 차지한다"는 부분은 과장이었다. 바코드 진입점은 이미 이미지 바로 아래 첫 동작으로 있었다(`CouponScreens.kt:1089`). 다만 `Card` + `ListItem` 형태여서 아래쪽 `예약`·`사용 완료`(채워진 `Button`)보다 시각적으로 약했다. 즉 순서 문제가 아니라 **강조 수준 문제**였다.

## 6. 표준 제스처·피드백 부재

| 어포던스 | 등장 횟수 | 영향 |
| --- | --- | --- |
| ~~`PullToRefresh`~~ | ~~0~~ | **(2026-08-28 정정)** 결함이 아니다. 모든 목록이 Firestore `addSnapshotListener` 실시간이거나 명시적 새로고침 버튼을 갖고 있다. 상세는 아래 참조 |
| ~~`SwipeToDismiss`~~ | ~~0~~ | **(2026-08-28 정정)** 추가하지 않기로 판단했다. 근거는 이 절 아래 참조 |
| 스켈레톤(`shimmer`/`placeholder`) | 0 | 로딩이 `CircularProgressIndicator` 10곳뿐 |
| `HapticFeedback` | 0 | 바코드 감지·사용완료 성공에 촉각 확인 없음 |

**(2026-08-28 정정)** "다른 멤버가 쿠폰을 등록했을 때 목록에 반영시키는 사용자 조작이 존재하지 않는다"고 적었으나 사실과 다르다. 사용자 조작이 필요 없다 — 자동으로 반영된다.

| 목록 | 갱신 방식 | 근거 |
| --- | --- | --- |
| 쿠폰 목록 | 실시간 | `CouponPager.kt:102` `query.addSnapshotListener` |
| 내 방 목록 | 실시간 | `RoomRepository.kt:33` `observeMemberships` |
| 방 정보·멤버 | 실시간 | `RoomRepository.kt:45,57` |
| 공개 방 목록 | 진입 시 자동 + 수동 버튼 | `RoomScreens.kt:439` `LaunchedEffect`, `:467` 새로고침 `TextButton` |

목록 로딩 실패 시 복구 경로도 이미 있다. `CouponPagingFooter`가 "목록을 더 불러오지 못했어요 / 다시 시도"를 렌더링하고 `retryCoupons()`에 두 지점이 연결돼 있다(`RoomScreens.kt:619,629`).

### PullToRefresh 를 추가하지 않기로 한 이유

관행상 사용자는 목록에서 아래로 당기는 동작을 시도한다. 그 기대를 충족한다는 가치는 실재한다. 그러나 이 저장소에서는 손해가 더 크다.

`CouponPager.refresh()`는 `pages.values.flatten().forEach { it.registration?.remove() }` 로 모든 snapshot listener 를 해제하고 `attachPage`로 다시 붙인다(`CouponPager.kt:58-70`). 실시간 목록에서 이미 최신인 데이터를 위해 리스너를 전부 재생성하는 것이므로, 당길 때마다 Firestore 읽기가 추가로 발생한다. 즉 당김 새로고침은 이 구조에서 **비용만 늘리고 얻는 것이 없는 no-op**에 가깝다.

당김 제스처가 정말 필요하다고 판단되면, 리스너를 재생성하지 않고 "이미 최신입니다" 피드백만 주는 형태로 별도 설계해야 한다. 지금 상태에서 `PullToRefreshBox`를 그대로 얹는 것은 회귀다. 스켈레톤 부재는 이미 확보한 성능 개선(P50 개선 기록)을 체감으로 전달하지 못하게 만든다 — 스피너는 진행률 정보가 없어 실제보다 느리게 느껴진다.


### SwipeToDismiss 를 추가하지 않기로 한 이유

실제 사용 흐름은 **쿠폰 열기 → 계산대에서 바코드 보여주기 → 사용 완료** 다. 사용자는 이미 상세 화면에 있고 `사용 완료` 버튼도 거기에 있다. 목록 스와이프는 주 경로를 한 단계도 줄이지 못한다.

구현 비용도 계층을 넘는다. `markUsed` 는 `CouponDetailViewModel` 에만 있고(`ViewModels.kt:896`), 5분 실행 취소 상태는 상세 화면 로컬 `rememberSaveable`(`CouponScreens.kt:926`)이다. 목록에 스와이프를 붙이려면 `RoomDetailViewModel` 에 동작 추가, 방 화면에 스낵바 호스트 추가, 실행 취소 효과 중복 구현이 필요하다.

삭제는 더 분명하다. 30일 soft-delete 로 복구는 가능하지만, 드물게 쓰는 동작을 목록 최상위에서 한 번의 제스처로 노출할 이유가 없다.

목록 단위 정리가 목표라면 올바른 형태는 per-row 스와이프가 아니라 **다중 선택**이다(현재 `selectionMode` 관련 코드 0건). 이는 UI 다듬기가 아니라 별도 기능이므로 이 문서의 범위를 넘는다.

### 배지·상태 문구 중복 (2026-08-28 발견, 자체 유발)

PR #28 에서 목록에 `GDExpiryBadge` 를 넣으면서 `supportingContent` 의 `statusLabel` 을 그대로 두었고, PR #35 에서는 상세 화면에 `GDBadge(statusLabel)` 을 만료 배지 옆에 나란히 놓았다. 두 함수의 반환값이 종료 상태에서 겹치는 것을 확인하지 않은 결과다.

| 상태 | 배지 (`couponDdayLabel`) | 본문 (`statusLabel`) | 결과 |
| --- | --- | --- | --- |
| used | 사용 완료 | 사용 완료 | 같은 값 2회 |
| expired | 만료 | 만료됨 | 사실상 같은 값 2회 |
| active | D-7 | 사용 가능 | 중복 아님 |
| reserved | D-7 | 예약됨 | 중복 아님 |

`expiryBadgeAlreadyStatesStatus(urgency)` 로 종료 상태에서만 상태 문구를 빼도록 고쳤다. 진행 중에는 배지가 기간만 말하므로 상태 문구가 여전히 정보를 더한다.

## 7. 접근성 잔여 결함

| 항목 | 측정 | 문제 |
| --- | --- | --- |
| ~~`contentDescription = null`~~ | ~~19 / 40 (47.5%)~~ | **(2026-08-28 정정)** 결함이 아니다. 20건 전수 확인 결과 모두 인접 텍스트가 같은 의미를 전달하는 장식 아이콘이었다. M3에서 장식 아이콘은 `null`이어야 하며, 설명을 붙이면 "로그인 로그인"처럼 이중 announce가 발생한다. 반대편도 확인했다 — 아이콘 전용 컨트롤 20건(뒤로·로그아웃·알림 설정·멤버·방 설정·축소·확대·닫기·바코드 닫기·만료일 달력, 이미지 6건, 스위치 1건, 접힘/펼침 조건식 3건)은 **전부** 설명이 있다. 47.5% 비율 자체는 결함 신호가 아니었다 |
| `heading` semantics | 0 | TalkBack 제목 단위 탐색 불가 |
| ~~`SemanticsProperties` 직접 지정~~ | ~~0~~ | **(2026-08-28 정정)** 측정 오류였다. `SemanticsProperties`는 테스트 측 클래스명이라 소스에서 0건인 것이 정상이다. 소스는 프로퍼티 세터 `stateDescription`을 쓰며 실제로 **3곳**에 있다 — 확대 배율(`CouponScreens.kt:1671`), 스위치 켜짐/꺼짐(`SettingsScreens.kt:291`), 접힘/펼쳐짐(`RoomScreens.kt:898`). 결함이 아니다 |
| 44dp 터치 타겟 | `RoomScreens.kt:338` | 48dp 최소 기준 미달 |

`contentDescription = null`은 장식용 아이콘에는 올바른 선택이다. 최초 검토에서 `Common.kt:116`을 "카드의 유일한 의미 전달 아이콘"으로 지목했으나 이것도 틀렸다. 해당 지점은 빈 상태·로딩 상태의 일러스트레이션이고 바로 아래에 `title`과 `message` 텍스트가 함께 렌더링된다. 순수 장식이 맞다.

전수 재판정 결과 이 저장소의 `contentDescription` 운용은 이미 정확하다. 남은 작업은 이 불변식이 깨지지 않도록 회귀 테스트로 고정하는 것뿐이다.

200% 글꼴 실측 결과는 양호했다 — 로그인 화면에서 잘림·겹침·버튼 소실이 없고 정상 리플로우됐다. `TODO_v1.1.md`의 `[x]` 주장은 최소한 로그인 화면에서 사실이다.

## 8. 문자열 하드코딩

```
stringResource 호출: 0
res/values/strings.xml 항목: 1
Kotlin 내 한글 리터럴: 712
```

712개 문구가 Compose 코드에 직접 박혀 있다. 세 가지 실질적 손실이 있다.

- 문구 QA를 코드 diff로만 할 수 있어 비개발자 검수가 불가능하다
- 동일 개념의 표현이 갈라진다(`없습니다`/`없어요` 혼용 22곳 확인)
- 다국어 확장 시 712곳을 손대야 한다

국내 전용이라면 다국어는 접어도 되지만, **문구 일관성 관리**만으로도 추출 가치가 있다.

## 9. 변경 목록 (우선순위)

### P0 — 출시 차단

1. `primary`·`outline`·`error`·`tertiary` 대비 수정 (1절 권장값 적용). 파일: `ui/theme/GDTheme.kt`
2. `darkColorScheme` 추가 + `isSystemInDarkTheme()` 분기. 전체화면 쿠폰의 밝기 강제와 상호작용 회귀 확인
3. 쿠폰 목록·상세에 **D-day 배지** 도입, 만료 임박 색+아이콘+텍스트 3중 인코딩
4. `(Asia/Seoul)` 등 내부 식별자 UI 노출 제거

### P1 — 실사용 신뢰도

5. `Typography` 정의: 한글 `lineHeight` 1.5배, `LineBreak.Paragraph`
6. `Shapes` 위계 복원: `medium 12 / large 16 / extraLarge 24`
7. 쿠폰 상세 재구성: 이미지 + D-day → CTA `사용하기` 단일화 → 부가정보 접이식
8. ~~`PullToRefresh` 도입~~ → 전 목록이 실시간이라 추가하지 않음. 리스너 재생성 비용만 발생 (정정 근거는 6절)
9. 목록 스켈레톤 도입, 스피너 대체
10. ~~`contentDescription = null` 19곳 개별 재판정~~ → 전수 재판정 완료, 결함 아님. 불변식 회귀 테스트로 대체
11. `heading` semantics 부여 (화면별 최상위 제목)
12. `RoomScreens.kt:338` 44dp → 48dp

### P2 — 세련도

13. ~~`SwipeToDismiss`로 사용완료·삭제 단축~~ → 추가하지 않음. 주 사용 흐름이 이미 상세 화면에서 끝나 목록 스와이프가 경로를 줄이지 못한다. 대신 발견한 상태 중복 결함을 수정 (6절)
14. 바코드 감지 성공·사용완료에 `HapticFeedback`
15. ~~Material You 동적 색상 옵션~~ → 도입하지 않음. 대비 검증 무효화·스켈레톤 파손·의미 기반 색과의 충돌 (2절 참조). 대신 테마 override 전파 결함을 수정
16. 712개 문구 `strings.xml` 추출과 표현 통일

## 10. 비관적 관점

색 토큰 변경은 스크린샷 기반 회귀 테스트가 없으면 전 화면 시각 회귀를 유발한다. 현재 저장소에 스크린샷 비교 테스트가 없어서, 대비 수정 PR은 사람이 화면을 하나씩 확인해야 한다. 다크 테마 추가는 색 토큰 2배 + 이미지 위 텍스트·전체화면 밝기 강제와의 상호작용까지 늘어나므로, 지금 규모에서 P0 4개를 한 PR에 묶으면 검수가 붕괴한다. **PR을 결함 단위로 쪼개는 것이 전제다.**

D-day 도입은 서버 만료 상태 갱신 배치(`TODO_v1.1.md` P1 미완)와 결합된다. 클라이언트가 D-day를 계산해 보여주는데 서버 상태가 `active`로 남아 있으면 "D-0인데 사용 가능"처럼 모순된 화면이 나온다.

## 11. 긍정적 관점

본문 색(16.65:1)과 보조 텍스트(5.36:1)는 이미 넉넉히 통과하므로, 이번 수정은 **강조색 6개 값 교체**로 대부분 끝난다. 색상이 `GDTheme.kt` 한 파일에 모여 있고 화면 코드가 전부 `MaterialTheme.colorScheme` 참조를 쓰고 있어, 하드코딩 색을 찾아다닐 필요가 없다. 200% 글꼴 리플로우가 이미 견디고 `testTag` 53개가 깔려 있어 회귀 테스트를 붙일 지점도 확보돼 있다. D-day 배지와 다크 테마는 스토어 스크린샷 경쟁력에 직접 기여하는 변경이기도 하다.

# 변경 이력

이 프로젝트는 공개된 버전을 덮어쓰지 않고 새 `versionName`과 `versionCode`로 발행합니다.

## Unreleased — 0.1.28 (29)

### UI/UX

- 단일 쿠폰이나 일괄 등록 마지막 장에서 뒤로가기를 눌러도 선택 이미지와 입력값을 즉시 버리지 않고 확인
- `계속 입력`은 현재 폼을 그대로 유지하고, `입력 버리고 나가기`를 명시적으로 선택한 경우에만 등록 큐를 정리
- 이미지가 없는 빈 등록 화면은 추가 확인 없이 바로 나가며, 업로드 중·남은 묶음 취소 확인의 기존 우선순위 유지

### 안정성·검증

- 뒤로가기 정책을 업로드 중·남은 묶음·미저장 초안·빈 화면의 4단계 순수 상태로 분리
- 단일·마지막 초안 보호와 빈 화면 즉시 이탈 단위 회귀, 확인창 유지·폐기 Compose 계측 회귀 추가
- Android 단위 102/102, Android 16 AVD 계측 52/52, lint 오류 0건, R8 release/benchmark build 통과
- Firestore Rules 24/24, Backend Vitest 58/58·도구 4/4, typecheck·Next production build와 npm High/Critical 0건 회귀 통과
- GitHub signing dry-run `31884412198` 성공: signed APK 47,012,293 bytes, SHA-256 `f1569432c76004974696615fa9af3c59f347edc267eb095f8fd0243bc4e4f266`, 새 RSA 4096bit 인증서·v2/v3·ZIP alignment 일치

## 배포 대기 — 0.1.27 (28)

### UI/UX

- 쿠폰 등록 중 화면 회전·Activity 재생성 뒤에도 이름·브랜드·만료일·바코드와 공유 옵션을 복원
- 사용자가 한 번 수정한 필드는 늦게 끝난 OCR·바코드 분석이 다시 덮어쓰지 않고 수동 입력을 최종값으로 유지
- 다음 이미지로 이동하면 새 이미지용 초깃값으로 분리해 이전 쿠폰의 수정값이 일괄 등록 다음 장에 섞이지 않도록 제한

### 안정성·검증

- 자동 입력 전용 상태와 사용자 수정 여부를 한 객체로 분리해 화면의 개별 상태 분기 6개를 제거
- 자동 입력·수동 수정 경쟁, 바코드 제외 후 재감지 차단 단위 테스트 3개와 saved-state 복원 Compose 계측 1개 추가
- Android 단위 101/101, Android 16 AVD 계측 51/51, lint 오류 0건, R8 release/benchmark build 통과
- Firestore Rules 24/24, Backend Vitest 58/58·도구 4/4, typecheck·Next production build와 npm High/Critical 0건 회귀 통과
- GitHub signing dry-run `31882944090` 성공: signed APK 47,012,293 bytes, SHA-256 `a29e633f005b0d84742e8b3fdf7ba36c124285cea5effd1a152377487c6df1ac`, 새 RSA 4096bit 인증서·v2/v3·ZIP alignment 일치

## 배포 대기 — 0.1.26 (27)

### UI/UX

- 일괄 등록에서 현재 쿠폰을 검토하는 동안 다음 이미지 1장의 OCR·바코드 분석과 빠른 업로드 준비를 미리 수행
- 다음 쿠폰의 `미리 읽는 중`·자동 입력 준비·빠른 업로드 준비 상태를 남은 장수 아래에 간결하게 표시
- 다음 이미지로 이동할 때 완료된 선처리 결과를 즉시 적용하고, 부분 실패 시 성공한 자동 입력 또는 준비 파일은 보존

### 성능·안정성

- 선처리 상한을 다음 1장으로 고정해 최대 10장 묶음도 전체 bitmap·임시 파일을 동시에 만들지 않음
- 현재 이미지 분석·준비가 모두 끝난 뒤에만 선처리를 시작하고, 현재 쿠폰 등록이 시작되면 미완료 작업을 취소해 전송과 CPU·I/O가 경쟁하지 않도록 제한
- 이미지 제외·묶음 교체·화면 종료·취소에서 소유권을 넘기지 않은 준비 파일을 닫고, 완료된 다음 장만 안전하게 승격

### 개발·검증

- 선처리 시작 정책·중복 방지·부분 완료 문구·가상 800ms 준비시간 은닉 단위 테스트와 진행/완료 Compose 회귀 추가
- Android 단위 97/97, Android 16 AVD 계측 50/50, lint 오류 0건, R8 release/benchmark build 통과
- Firestore Rules 24/24, Backend Vitest 58/58·도구 4/4, typecheck·Next production build와 npm High/Critical 0건 회귀 통과
- GitHub signing dry-run `31881438894` 성공: signed APK 47,012,293 bytes, SHA-256 `4b2d2f67dd4cba0a662332358dae05b4b1566566cb6293e798758cc9099c08ee`, 새 RSA 4096bit 인증서·v2/v3·ZIP alignment 일치

## 배포 대기 — 0.1.25 (26)

### UI/UX

- 업로드 전에 같은 만료일의 방 공개 쿠폰과 내 비공개 쿠폰을 최대 40건만 확인하고, 이름·만료일 또는 바코드가 일치하면 후보 최대 3개를 경고
- 오탐이나 동일 상품 여러 장을 고려해 자동 차단하지 않고 `돌아가서 확인`과 `그래도 등록`을 제공
- 중복 확인 중 뒤로가기는 조회 취소 범위와 입력·준비 이미지 유지 여부를 안내

### 보안·성능

- 다른 멤버의 비공개 쿠폰은 조회 대상에서 제외하고 Firestore Rules Emulator로 소유자 쿼리 경계를 검증
- 전체 쿠폰을 읽지 않고 기존 만료일 composite index를 재사용해 공개 20건+본인 비공개 20건으로 읽기 상한 제한
- 중복 조회 실패는 등록 핵심 흐름을 막지 않되 사용자에게 확인 실패를 알리고, 취소 예외는 실패 fallback과 분리해 업로드가 이어지지 않도록 차단

### 개발·검증

- 이름 정규화·바코드 우선순위·만료일 격리·브랜드 허용·결과 상한 단위 테스트 추가
- 중복 경고·강제 등록·조회 취소와 개인정보 쿼리 경계 자동 회귀 추가
- Android 단위 93/93, Android 16 AVD 계측 49/49, Firestore Rules 24/24와 Backend 58/58 통과
- GitHub signing dry-run `31879836609` 성공: signed APK 47,012,293 bytes, SHA-256 `d3fed65b1df27001ac6db3069d3f011502670198643f8ba1b59ebe3663a825a3`, 새 RSA 4096bit 인증서·v2/v3·ZIP alignment 일치

## 배포 대기 — 0.1.24 (25)

### UI/UX

- 이미지 준비·업로드 중 뒤로가기를 무반응으로 막는 대신 `취소하고 나가기` 확인과 단계별 정리 범위를 안내
- Firestore 저장·취소 정리 단계에서는 중간 취소가 불가능한 이유와 다음 행동을 명시

### 안정성

- 준비·전송 단계에서 취소 요청이 실제 수락된 경우에만 정리 완료 후 화면을 이탈해 저장 완료·상세 이동과의 경쟁 차단
- 전송 취소 뒤 ViewModel의 준비 파일 정리와 상위 등록 큐 URI/cache 정리를 순서대로 완료

### 개발·검증

- 업로드 단계별 취소 가능 정책 단위 테스트 2개와 전송 취소·저장 차단 Compose 계측 테스트 2개 추가
- Android 단위 86/86, Android 16 AVD 계측 46/46 통과

## 배포 대기 — 0.1.23 (24)

### UI/UX

- 일괄 등록에서 잘못 고른 현재 이미지를 확인 후 제외하고 바로 다음 쿠폰으로 이동하는 동작 추가
- 두 장 이상 남은 상태에서 상단·시스템 뒤로가기를 누르면 남은 장수를 보여 주고 전체 취소 여부를 확인
- 다음 이미지로 이동할 때 화면을 맨 위로 복원하고 이름·브랜드·바코드·만료일만 초기화하며 공유·알림 선택은 유지

### 성능·안정성

- 이미지마다 만들던 등록 `ViewModel`·Repository·HTTP 리소스를 일괄 등록 전체에서 1개만 재사용해 최대 10개→1개로 제한
- 저장·제외 즉시 현재 gallery URI 접근 권한 또는 공유 cache를 정리하고, 다음 분석 시작 전에 이전 결과·업로드 상태를 동기 초기화
- 분석 결과에 원본 URI를 결합해 느리게 끝난 이전 이미지의 OCR·바코드가 다음 폼을 덮어쓰지 않도록 차단

### 개발·검증

- 일괄 등록 뒤로가기 정책 단위 테스트 4개와 이미지 제외·전체 취소 Compose 계측 테스트 2개 추가
- Android 단위 84/84, Android 16 AVD 계측 44/44 통과

## 배포 대기 — 0.1.22 (23)

### UI/UX

- 갤러리에서 이미지를 최대 10장까지 한 번에 선택하고 `1/N` 진행 상태로 한 장씩 OCR·바코드·만료일을 확인해 저장
- Android 공유 시트의 `ACTION_SEND_MULTIPLE`을 받아 로그인·쿠폰방 선택 뒤 같은 순차 확인 등록 큐로 연결
- 각 쿠폰 저장 버튼을 `저장하고 다음 쿠폰 확인`·`마지막 쿠폰 등록하기`로 구분하고 가져오기 진행률을 장수로 표시

### 성능·안정성

- 다중 공유 이미지를 전체 메모리에 올리지 않고 기존 64KB 스트리밍 복사를 순차 적용해 peak 메모리를 단일 이미지 수준으로 제한
- 장당 10MB·4천만 화소와 별도로 묶음 전체 50MB·10장 상한을 두고, 한 장이라도 검증 실패하면 이미 복사한 묶음을 모두 정리
- 갤러리 URI 접근 권한과 앱 전용 공유 cache를 등록·취소·묶음 교체 시 정리하고 Activity 재생성 후 준비된 전체 묶음을 복원

### 개발·검증

- Android 단위 80/80, Android 16 AVD 계측 42/42, 다중 공유 3장 복사·순서·진행률·회전 복원·부분 실패 전체 정리·200% 글꼴 회귀 통과
- lint 오류 0건·경고 14건, R8 release/benchmark build와 확대 핀치 5회 성능 회귀 통과

## 배포 대기 — 0.1.21 (22)

### UI/UX

- Android 사진 앱·갤러리의 공유 시트에서 이미지 1장을 기프티콘닥터로 바로 가져오는 등록 진입점 추가
- 로그인 전에는 공유 이미지를 앱 전용 cache에 보관하고, 로그인 후 쿠폰방 선택 → 정보 확인 → 저장 순서로 연결
- 공유 이미지 준비·오류·취소 상태를 로그인과 쿠폰방 화면에서 명시하고 200% 글꼴 접근성 회귀 추가

### 보안·안정성

- 외부 공유는 `ACTION_SEND`·`image/*`·`content://` 조합만 허용하고 실제 이미지 헤더·크기를 다시 검증
- 공유 파일을 64KB 버퍼로 최대 10MB까지만 복사하고 반복된 빈 읽기, 빈 파일, 경로 탈출을 차단
- 원본 URI 임시 권한에 의존하지 않도록 앱 소유 cache로 즉시 복사하고 취소·완료 또는 24시간 경과 뒤 정리
- Activity 재생성 중 공유 파일을 중복 복사하거나 대기 중 딥링크를 잃지 않도록 상태 복원

### 개발·검증

- 공유 Intent 정책·10MB 상한·빈 읽기 단위 테스트와 MediaStore 복사·실제 MainActivity `ACTION_SEND`·재생성 계측 테스트 추가
- Android 16 AVD에서 공유 대상 노출과 전체 단위 78/78·계측 39/39·lint 오류 0건 검증

## 배포 대기 — 0.1.20 (21)

### 성능

- 이미지 HTTP 요청을 코루틴 취소와 연결해 화면 이탈 시 다운로드를 즉시 중단
- Android 업로드 읽기 버퍼를 8KB→64KB로 늘리고 서버 원본 업로드와 썸네일 변환·업로드를 병렬화
- 서버는 전체 원본 복사를 기다리지 않고 앞 12바이트 형식 검증 직후 `File` Blob 전송을 시작하며, 전체 Buffer는 썸네일 처리에만 사용
- 업로드·교체·이미지 조회 응답에 단계별 `Server-Timing`을 추가하고 비파괴 health/thumbnail/original HTTPS 벤치마크 도구 제공
- 이미지 선택 직후 OCR과 업로드 전처리를 병렬 실행하고, 1.5MB 이상·2,560px 초과 이미지는 JPEG 92로 준비하되 10% 이상 작을 때만 사용
- OCR·바코드 자동 입력을 더 느린 업로드 전처리 완료와 분리해, 분석이 먼저 끝나면 필드를 즉시 확인·수정할 수 있게 개선
- 전송 실패·취소 시 이미 준비한 최적화 파일을 유지해 재시도에서 537~598ms 전처리와 JPEG 재생성을 반복하지 않도록 개선
- 업로드 전후 용량과 준비·전송·저장 단계를 분리 표시하고 취소·이미지 재선택·앱 재시작 때 전처리 임시 파일 정리
- 상세 이미지 교체도 선택 직후 미리 최적화하고 준비 중 확인 차단·재선택/취소·준비 완료 용량 안내 적용
- 클라이언트 upload session ID와 결정적 후보 경로를 도입해 응답 유실 Blob을 추적 가능하게 변경
- 동일 쿠폰 이미지의 동시 요청을 표시 크기와 무관하게 직렬화하고 메모리 캐시를 재확인해 중복 다운로드 억제
- 이미지 요청 gate를 참조 수 기반으로 정리해 대기 중 새 요청이 별도 다운로드를 시작하는 경쟁 조건 차단
- 압축 썸네일을 2~8MB 메모리 LRU에 보관해 목록의 작은 이미지에서 상세 미리보기로 전환할 때 네트워크 재요청 없이 목표 크기만 다시 디코딩
- 상세 원본을 최대 10MB `ByteArray`로 보관하지 않고 앱 전용 cache 파일에 64KB씩 스트리밍한 뒤 파일에서 미리보기·확대 디코딩
- 썸네일이 있는 쿠폰은 상세 진입 시 원본 요청을 1회→0회로 지연하고 확대 탭·썸네일 실패 때만 다운로드하며, 확대를 닫으면 진행 중 요청 취소
- power-of-two 샘플 bitmap을 실제 표시 영역까지 축소해 24개 목록 bitmap cache를 5,529,600B→1,990,656B(64.0%)로 절감
- LazyColumn 항목에 안정 키와 `contentType`을 제공하고 R8 release 축소·리소스 최적화 활성화
- 100개 쿠폰 목록 cold start·스크롤 Macrobenchmark와 Android 16 UI 테스트 추가
- 실제 시작·100개 목록 경로의 Baseline/Startup Profile을 생성하고 적용 전·후 3쌍(6개 테스트) 성능 비교 추가
- 실제 로그인 화면 cold start A/B를 추가하고 시작·목록 규칙 563개를 합쳐 선행 컴파일 범위 보강
- 확대 이미지를 백그라운드에서 디코딩하고 실제 뷰포트 기준으로 이동 범위 제한
- 전체화면 원본을 먼저 화면 크기 1×로 디코딩하고 실제 확대 시에만 2×를 준비해 최초 bitmap을 12,441,600B→3,110,400B로 75.0% 절감
- 1.5× 미만의 작은 핀치는 1× bitmap으로 즉시 반응하고, 의도한 확대에서만 2× 준비를 시작해 불필요한 12,441,600B bitmap 디코드 방지
- 핀치 중 배율·이동 상태를 lambda형 `graphicsLayer`에서 읽도록 지연해 동일 profile 6회 왕복×5회에서 CPU frame P90 11.65→9.83ms, P95 21.27→11.00ms로 단축
- 실제 확대 다이얼로그 멀티터치 Macrobenchmark와 profile 수집 여정을 추가하고 앱 전용 baseline 규칙을 563개→826개로 보강
- Vercel Function 4.5MB 본문 제한에 맞춰 10MB 이하 입력을 최대 4MiB payload로 적응 압축하고, 크기 미상 입력은 제한 스트리밍 복사 후 작은 원본을 재압축 없이 유지
- 5.63MB 고노이즈+CODE128 샘플을 4.59MB(q92 단일) 대신 4.00MB로 준비하면서 2,560×2,560 해상도와 바코드 재감지를 유지
- 스크롤 Macrobenchmark의 Activity 시작을 측정 구간에서 분리해 순수 fling 기준으로 교정
- 복구함을 20개 cursor paging으로 전환해 첫 요청 문서 상한을 일반 멤버 21개·방장 42개로 제한

### UI/UX

- 쿠폰 삭제를 즉시 영구 삭제에서 30일 복구함으로 전환하고 방 목록에서 `실행 취소` 제공
- 방 설정에 최근 삭제한 쿠폰 화면을 추가해 남은 보관일 확인·복원·확인 후 영구 삭제 지원
- 이미지 두 번 탭 시 탭한 지점을 중심으로 2배 확대하고 다시 두 번 탭하면 원위치
- 이미지 전체 화면에 1×~4× 확대·축소와 원본 맞춤 조작 추가
- 전체화면 쿠폰 표시 중 최대 밝기·화면 켜짐을 적용하고 닫을 때 사용자 이전 상태 복원
- 선택한 이미지에서 바코드를 오프라인 감지하고 값을 저장 전에 수정하거나 제외하는 확인 단계 추가
- 자동 감지 실패 시 6개 주요 형식의 바코드 값을 직접 입력하고 저장 전에 재생성 가능 여부를 검증
- 저장한 바코드를 계산대용 흰 배경 전체화면으로 재생성하고 최대 밝기로 표시
- 등록자가 기존 쿠폰 이미지를 미리 확인하고 진행률과 함께 교체하는 흐름 추가
- 로그인 전에 뜨던 알림 권한 창을 제거하고 첫 쿠폰방 이후 이유를 확인하고 허용하는 흐름으로 변경
- 쿠폰 등록 성공 후 상세 화면에서 완료를 확인하고 `하나 더 등록`을 바로 시작하는 스낵바 추가
- 자동 입력과 업로드 준비 상태를 분리해 안내하고, 한쪽이 먼저 완료되거나 실패해도 다른 결과를 유지
- 전체화면 확대를 썸네일 미리보기로 즉시 열고 고해상도 원본이 준비되면 같은 화면에서 교체하며, 실패 시 미리보기를 유지한 채 재시도 제공
- 전체화면 이미지를 한 번 탭해 안내·확대 조작부를 숨기거나 다시 표시하고, 확대 요청 중 선명도 준비 상태를 구분해 안내
- TalkBack에서 전체화면 이미지의 현재 배율과 조작부 표시·숨기기 액션을 읽고 실행할 수 있도록 접근성 semantics 추가
- 사용 완료 직후 5분 복구 창의 `실행 취소` 스낵바를 제공하고 처리자 본인만 사용 가능 상태로 복원
- 복구함 스크롤 끝에서 다음 20개를 선조회하고 추가 로딩·오류 재시도·목록 끝 상태를 구분
- 쿠폰 현황을 한 줄 요약으로 기본 접어 검색·필터·첫 쿠폰을 우선 노출하고, 펼치면 역할·알림 시간·통계를 확인하도록 정리
- 시스템 글꼴 150% 이상에서는 펼친 통계 4개를 세로로 재배치해 텍스트 잘림과 좁은 터치 영역을 방지
- 로그인·알림 설정·방 설정·멤버 관리의 핵심 동작을 시스템 글꼴 200%에서도 스크롤로 모두 접근 가능하게 고정
- 알림 스위치에 TalkBack용 한국어 이름·켜짐/꺼짐 상태를 명시하고 큰 글꼴에서는 라벨과 스위치를 세로 배치
- 초대코드 재발급은 방장에게만 노출하고, 멤버 제거는 대상 이름을 확인한 뒤 실행하도록 변경
- 캐시 재사용 계측은 단일 debug 스크롤 시간 비교 대신 fetch·decode 불변과 cache hit 수로 판정하고 시간값은 5회 Release/R8 Macrobenchmark와 진단 로그로 분리

### 개발·검증

- 분실된 v0.1.12 서명키 대신 새 RSA 4096bit 인증서로 전환하고, Actions 기본 실행을 tag·Release·대시보드를 변경하지 않는 signing dry-run으로 분리
- GitHub Secrets의 Firebase project/package/Web OAuth client와 keystore·signed APK 인증서 SHA-256을 배포 전에 이중 검증
- Android 단위 테스트 28개→58개, Android 계측 테스트 0개→27개
- Backend 단위 테스트 58개·API 벤치마크 스크립트 테스트 4개와 Firestore Rules·복구함 저장소 통합 테스트 23개로 30일 만료·권한·댓글 보존·비공개 격리·105개 cursor paging 검증
- 24개 썸네일 miss/hit·payload·PSS 5회 기준선과 동일 key 24개 동시 요청 fetch 1회 회귀 추가
- 같은 썸네일의 56×56→512×360 전환에서 압축 cache hit와 fetch 1회를 검증하고, 확대 화면의 미리보기→원본 교체 UI 회귀 추가
- 파일 응답 스트리밍·10MB 초과·취소 시 부분 파일 삭제 단위 테스트와 3,000×2,000 파일 디코드 성능 회귀 추가
- 실제 RoomDashboard의 24개 썸네일 순·역방향 스크롤에서 재구성 중 fetch·decode가 늘지 않는 UI 회귀 추가
- 실제 `CouponImageDialog`에 UIAutomator 핀치 열기·닫기 6회를 수행하는 Release/R8 Macrobenchmark와 Perfetto 재구성 비교 추가
- AndroidX Test를 JUnit 1.3.0·Espresso 3.7.0으로 갱신해 Android 16 테스트 호환성 확보
- CI에서 debug뿐 아니라 R8 release, UI test APK, Macrobenchmark APK 컴파일을 차단 게이트로 추가
- 새 benchmark 모듈의 `build/` 생성물을 Git에서 제외해 실제 신규 소스와 생성 파일이 섞이지 않도록 정리
- 사용 완료 복원 Rules의 본인 허용·타인 거부·5분 만료를 포함해 Firestore Emulator 17개 회귀 검증
- 시스템 글꼴 200%에서 쿠폰 현황·검색·필터·첫 쿠폰의 48dp 실제 터치 영역과 접근 경로를 고정하고 전체 계측 30/30 검증
- 이미지 없는 쿠폰 행은 이미지 상태·코루틴을 만들지 않고 카테고리 아이콘을 즉시 표시
- 로그인·알림·방장/멤버 설정·멤버 제거 200% 접근성 계측 5개를 추가해 전체 Android 계측 35/35 검증
- OCR·업로드 준비의 독립 완료·실패·상위 취소를 단위 3개로 고정하고, 분리된 진행 상태 전환을 Compose 계측 1개로 추가
- 최종 Android 단위 72/72, 기본·시스템 글꼴 200% 전체 계측 각 36/36, lint 오류 0건과 R8 release 빌드 검증

### 보안

- 삭제된 쿠폰은 클라이언트 직접 읽기·이미지 접근을 차단하고 비공개 복구함은 등록자에게만 노출
- 만료된 복구함 문서는 일일 Cron이 최대 100개씩 영구 삭제하고 Blob cleanup queue로 실패를 재시도
- Cron secret 누락 시 비인증 실행을 허용하지 않도록 fail-closed 처리
- JPEG, PNG, WebP magic byte 검증으로 취약한 범용 이미지 파서 제거
- 쿠폰 ID와 Blob 경로를 서버·Firestore Rules 양쪽에서 결합 검증
- 방 생성·가입, 이미지 업로드, 테스트 푸시 등에 사용자별 rate limit 적용
- 로그아웃 시 FCM token 문서와 로컬 token 정리
- FCM token 정리에 실패하면 인증 세션을 유지해 다음 계정에 이전 사용자의 알림이 노출되는 fail-open 로그아웃 차단
- Next.js 16.3.0, Firebase Admin 14.2.0, Vitest 4.1.10으로 갱신
- production audit의 High/Critical 0건을 유지하고, 강제 Firebase Admin 다운그레이드가 필요한 간접 `uuid` Moderate 6건은 알려진 잔여 위험으로 기록

### 안정성

- 로그인할 때 기존 알림 설정이 기본값으로 덮이는 문제 수정
- 공개·비공개 쿠폰 쿼리 병합 경쟁과 최신 댓글 100개 조회 오류 수정
- OCR에서 발행일보다 명시된 만료일을 우선하도록 개선
- 손상된 날짜 문서가 전체 쿠폰 목록을 중단하지 않도록 격리
- 쿠폰 문서 저장 실패 시 업로드 Blob을 보상 삭제
- 업로드 응답 후 Firestore 저장 실패·화면 이탈 시 쿠폰 문서 존재를 재확인하고 미사용 Blob 삭제를 영속 cleanup queue로 보장
- Blob 저장 전 다음 일일 Cron용 cleanup 안전망을 만들고 Firestore 커밋 후 확정하며, 취소 시 10분 지연 정리와 즉시 삭제를 함께 요청
- 업로드 취소 중에는 재요청 버튼을 숨기고 임시 파일 정리 상태를 명시적으로 표시
- 상세 원본 cache 파일을 화면 종료·요청 취소·로그아웃·다음 앱 시작 시 삭제하고 삭제 실패 시 내용을 0바이트로 비운 뒤 재시도
- foreground·cold start 알림 deep link와 인증 초기화 순서 보강
- 백그라운드 FCM 알림의 `deepLink` extra를 복원하고 앱 전용 URI만 허용
- 앱 시작 시 만료 알림 채널을 미리 생성해 첫 백그라운드 푸시의 fallback 의존 제거
- 만료 푸시를 `쿠폰 만료 D-n` 형식으로 간결하게 정리하고 테스트 푸시의 불필요한 10초 대기 제거
- 방 가입·탈퇴·멤버 제거를 transaction으로 처리해 `memberCount` 정합성 보강
- 소유 쿠폰이 남은 멤버의 탈퇴·제거를 차단해 고아 쿠폰 방지
- 예약 쿠폰은 예약자만 사용 완료할 수 있고 완료 시 예약자 정보를 정리하도록 보강
- 쿠폰 등록자의 일반 정보 수정 권한에서 상태 필드를 제외해 허용된 상태 전이 우회 차단
- Cron 부분 실패를 HTTP 500과 summary로 노출해 무음 실패 방지
- 푸시 연결 테스트에서 알림 권한, FCM 기기 등록, 서버 전송 실패를 구분해 안내
- 쿠폰 목록 이미지를 표시 크기에 맞춰 샘플 디코딩하고 4~24MB LRU 캐시 적용
- 이미지 교체 시 등록자 권한과 동시 변경을 검증하고 실패한 새 Blob을 보상 삭제
- 이미지 교체 시 이전 이미지에서 감지한 바코드 메타데이터를 제거해 잘못된 코드 표시 방지
- 이미지 교체·쿠폰 삭제·방 삭제의 Blob 정리 작업을 영속 큐에 기록하고 live-reference 방어·5회 재시도·lease·dead-letter와 운영 상태 집계 추가

### UI/UX

- 쿠폰 이름·브랜드 검색과 전체·사용 가능·예약·사용 완료·만료 상태 필터 추가
- 사용 가능한 쿠폰을 만료 임박순으로 우선 표시하고 필터 결과 수 안내
- 사용 완료와 쿠폰 삭제 전에 영향 범위를 확인하는 절차 추가
- 댓글 입력과 쿠폰 수정 화면을 서버 성공 후에만 비우거나 닫도록 변경
- 쿠폰 등록·수정 만료일에 직접 입력과 달력 선택을 함께 제공
- Google 로그인을 첫 핵심 CTA로 배치하고 이메일 로그인을 보조 흐름으로 정리
- 빈 쿠폰방 화면에 만들기와 초대코드 입장 행동을 직접 제공
- 방 상세의 작은 추가 아이콘을 라벨이 있는 `쿠폰 등록` FAB로 변경
- 쿠폰 등록을 이미지 선택, 자동 인식 결과 확인, 저장의 3단계로 단순화
- 공유·알림 옵션을 접을 수 있게 하고 만료일 빠른 선택 제공
- 운영용 푸시 테스트방 참가 기능을 홈에서 알림 설정의 진단 영역으로 이동

### 개발·배포

- Firestore Rules·Indexes를 `main` 조상 commit으로만 복원하고 기본 검증, production 승인, cloud dry-run, 배포 evidence를 강제하는 rollback·roll-forward workflow 추가
- Android 목록 검색·상태 분류·정렬 경계 사례 단위 테스트 4개 추가
- Android 검색·상태 필터·달력 선택·복구함 자동 paging/수동 재시도 Compose 계측 회귀 추가
- Gradle 8.10.2 Wrapper와 배포 파일 SHA-256 검증 추가
- PR/main Backend·Android CI와 Firestore Rules Emulator 테스트 추가
- 릴리스를 build/test/sign 이후에 발행하고 dashboard 갱신을 별도 job으로 분리
- 기존 Release/tag 덮어쓰기 기능 제거
- GitHub Actions를 Node 24 기반 공식 릴리스의 고정 SHA로 전환

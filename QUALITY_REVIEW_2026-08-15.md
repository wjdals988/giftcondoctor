# v0.1.14 코드 품질 검토 보고서

검토일: 2026-08-15
대상: `0.1.13 (14)` → `0.1.14 (15)` Android 변경분

## 전체 평가: B+ (76/100)

`code-quality-reviewer`의 가중치로 수동 평가했습니다. 자동 스크립트는 Kotlin 프로젝트를 TypeScript ESLint 기준으로 검사했고, 자체 `awk` 점수 계산 오류로 실행에 실패했습니다. 따라서 해당 스크립트가 출력한 `0점`은 사용하지 않고 실제 Android lint·테스트·빌드·기기 실행 결과를 근거로 삼았습니다.

| 평가축 | 이전 | 현재 | 근거 |
|---|---:|---:|---|
| 가독성 | 64 | 65 | 필터·정렬을 순수 함수로 분리했지만 `CouponScreens.kt` 대형 파일은 남음 |
| 성능 | 69 | 72 | 화면에 표시할 목록만 계산하고 만료 임박순으로 정렬, paging은 미적용 |
| 명시적 I/O | 80 | 82 | 검색어·상태·기준일 입력과 결과 목록의 경계가 명확함 |
| 유지보수성 | 78 | 81 | Android 단위 테스트 14개→18개, 목록 규칙 경계 사례 4개 추가 |
| 에러 처리 | 76 | 82 | 댓글·수정 상태를 서버 성공 후 변경하고 중복 작업을 busy state로 차단 |
| 협업 | 92 | 94 | 기능·날짜 UX·버전 문서를 논리 커밋으로 분리 |

가중 총점은 `65×0.25 + 72×0.20 + 82×0.15 + 81×0.25 + 82×0.10 + 94×0.05 = 76.1`이며 반올림해 76점입니다. 이전 74점보다 2점 상승했습니다.

## 통과 항목

- Android 단위 테스트: 18/18 통과
- Android lint: issue 0건
- debug APK 빌드: 성공
- 생성 APK: `giftcondoctor-0.1.14-15-debug.apk`
- APK SHA-256: `a8bd6e33e049d0e4aaa056227250d1ca122a11add348c99cb3dbd24f55720225`
- `emulator-5556` 업데이트 설치: 성공
- 설치 버전: `0.1.14 (15)`
- cold start: `MainActivity` resumed 확인
- 실행 직후 `AndroidRuntime` crash: 0건

## 개선된 사용자 흐름

1. 쿠폰 이름과 브랜드를 한 입력창에서 검색합니다.
2. 전체·사용 가능·예약·사용 완료·만료 5개 상태로 좁힙니다.
3. 사용 가능한 항목은 가장 가까운 만료일 순으로 확인합니다.
4. 사용 완료와 삭제는 확인 대화상자를 거쳐 오작동을 줄입니다.
5. 댓글과 수정 폼은 서버 저장 실패 시 입력 상태를 보존합니다.
6. 만료일은 직접 입력과 달력 선택을 모두 지원합니다.

## 남은 위험

- 공식 v0.1.12 서명키가 없어 `0.1.14`를 기존 앱 위에 정식 업데이트할 수 없습니다.
- 실제 물리 디바이스가 아니라 `emulator-5556`에서 검증했으며, 로그인된 방 데이터로 새 목록 UI를 시각 검증하지 못했습니다.
- Android instrumented UI 테스트는 여전히 0개라 확인 대화상자와 달력 상호작용은 컴파일·lint까지만 자동 검증됩니다.
- 검색은 목록 전체를 메모리에 가진 상태에서 `O(n log n)`으로 정렬합니다. 서버 paging 전에는 쿠폰 수가 많을수록 초기 지연이 늘어납니다.
- 삭제는 확인 절차만 추가됐고 undo·tombstone·재개 가능한 Blob cleanup은 아직 없습니다.
- Google Sign-In deprecated API 경고 6건이 남아 Credential Manager 전환이 필요합니다.

## 다음 우선순위

1. [P0] 새 release keystore 전환 결정, Firebase 인증서 재등록, 재설치 공지
2. [P0] 물리 디바이스에서 FCM 6개 상태와 A→B 교차 계정 검증
3. [P1] 검색·필터·사용 완료·삭제·달력 Compose UI 테스트 추가
4. [P1] 썸네일 API와 Firestore cursor paging 도입
5. [P1] 삭제 tombstone과 cleanup 재시도, 사용 완료 undo 설계
6. [P2] `CouponScreens.kt`, `RoomScreens.kt`, `ViewModels.kt` 화면·상태 단위 분리

## 긍정적 평가

검색·상태 분류·정렬을 Compose 외부의 순수 함수로 분리해 날짜 경계와 검색 조건을 빠르게 회귀 검증할 수 있습니다. 사용자 입력을 낙관적으로 지우지 않고 서버 성공을 기다리는 방식으로 바꿔 네트워크 실패 시 재입력 비용도 줄였습니다.
